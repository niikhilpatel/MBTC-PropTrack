const KEY="proptrack_v2";
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
let state=loadLocal(),pendingDelete=null,cloudReady=false,sessionToken=null,syncing=false;

function uid(){return crypto.randomUUID?crypto.randomUUID():Date.now()+"-"+Math.random()}
function defaultState(){let id=uid();return{selectedId:id,accounts:[{id,firm:"Demo Prop Firm",name:"$100K Account",accountSize:100000,startingBalance:100000,targetType:"percent",targetInput:8,target:8000,maxDdType:"percent",maxDdInput:10,maxDd:10000,dailyDdType:"percent",dailyDdInput:5,dailyDd:5000,resetTime:"00:00",ddMode:"static",notes:"",trades:[]}]}}
function loadLocal(){try{let x=localStorage.getItem(KEY);if(x)return JSON.parse(x)}catch(e){}return defaultState()}
function saveLocal(){localStorage.setItem(KEY,JSON.stringify(state))}
function a(){return state.accounts.find(x=>x.id===state.selectedId)||state.accounts[0]}
function money(n){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:2}).format(Number(n)||0)}
function pct(n){return(Number(n)||0).toFixed(2)+"%"}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function bal(x){return x.startingBalance+x.trades.reduce((s,t)=>s+Number(t.pnl||0),0)}
function high(x){let b=x.startingBalance,h=b;[...x.trades].sort((p,q)=>new Date(p.date)-new Date(q.date)).forEach(t=>{b+=Number(t.pnl||0);h=Math.max(h,b)});return h}
function maxUsed(x){let base=x.ddMode==="trailing"?high(x):x.startingBalance;return Math.max(0,base-bal(x))}
function maxRem(x){return Math.max(0,x.maxDd-maxUsed(x))}
function dayKey(d=new Date()){let x=new Date(d);return x.toISOString().slice(0,10)}
function today(x){return x.trades.filter(t=>dayKey(t.date)===dayKey()).reduce((s,t)=>s+Number(t.pnl||0),0)}
function dailyRem(x){return Math.max(0,x.dailyDd+Math.min(0,today(x)))}
function dailyUsed(x){return Math.max(0,-today(x))}
function targetRemaining(x){return Math.max(0,x.target-(bal(x)-x.startingBalance))}
function accountStatus(x){
  const b=bal(x), profit=b-x.startingBalance, used=maxUsed(x);
  if(used>=x.maxDd) return ["danger","FAILED","Maximum drawdown hit"];
  if(profit>=x.target) return ["passed","PASSED","Profit target achieved"];
  return ["","SAFE","Account active"];
}
function statusBadge(x){const [c,t]=accountStatus(x);return `<span class="status ${c}">● ${t}</span>`}
function risk(r,l){if(r<=0)return["danger","Breached"];if(l&&1-r/l>=.75)return["warning","Caution"];return["","Safe"]}
function badge(c,t){return`<span class="status ${c}">● ${t}</span>`}
function fill(n,l){return l?Math.min(100,Math.max(0,n/l*100)):0}
function localDT(){let d=new Date();d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,16)}
function refreshSwitch(){let s=$("#accountSwitcher");s.innerHTML=state.accounts.map(x=>`<option value="${x.id}" ${x.id===state.selectedId?"selected":""}>${esc(x.firm)} — ${esc(x.name)}</option>`).join("")}

async function cloud(path,options={}){let r=await fetch("/.netlify/functions/api"+path,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});let data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Cloud error ${r.status}`);return data}
async function initCloud(){
  try{
    let deviceId=localStorage.getItem("proptrack_device_id");if(!deviceId){deviceId=uid();localStorage.setItem("proptrack_device_id",deviceId)}
    let d=await cloud("/init",{method:"POST",body:JSON.stringify({deviceId})});sessionToken=d.token;cloudReady=true;localStorage.setItem("proptrack_session",sessionToken);
    let remote=await cloud("/data",{headers:{Authorization:"Bearer "+sessionToken}});
    if(remote.accounts?.length){state={selectedId:state.selectedId,accounts:remote.accounts};if(!state.accounts.some(x=>x.id===state.selectedId))state.selectedId=state.accounts[0].id;saveLocal()}
    else if(state.accounts.length){await cloud("/sync",{method:"POST",headers:{Authorization:"Bearer "+sessionToken},body:JSON.stringify({accounts:state.accounts})});}
    refreshAll();toast("Cloud sync connected.");
  }catch(e){cloudReady=false;console.warn(e);toast("Offline mode — cloud sync unavailable.")}
}
async function syncCloud(){if(!cloudReady||syncing)return;syncing=true;try{await cloud("/sync",{method:"POST",headers:{Authorization:"Bearer "+sessionToken},body:JSON.stringify({accounts:state.accounts})})}catch(e){console.warn(e);toast("Saved locally; cloud sync failed.")}finally{syncing=false}}
function save(){saveLocal();syncCloud()}

function renderDashboard(){let v=$("#dashboardView"),x=a();if(!x){v.innerHTML='<div class="empty"><strong>No account</strong>Add an account first.</div>';return}let b=bal(x),mr=maxRem(x),mu=maxUsed(x),dr=dailyRem(x),tp=today(x),tr=targetRemaining(x),[mc,mt]=risk(mr,x.maxDd),[dc,dt]=risk(dr,x.dailyDd);v.innerHTML=`<div class="grid metrics"><div class="metric"><div class="label">Account Balance</div><div class="value">${money(b)}</div><div class="muted">${b-x.startingBalance>=0?"+":""}${money(b-x.startingBalance)} overall</div></div><div class="metric"><div class="label">Profit Target Remaining</div><div class="value">${money(tr)}</div><div class="muted">Target ${money(x.target)} · ${pct(fill(Math.max(0,b-x.startingBalance),x.target))} complete</div></div><div class="metric"><div class="label">Max DD Remaining</div><div class="value ${mc}">${money(mr)}</div><div class="muted">${money(mu)} used of ${money(x.maxDd)}</div></div><div class="metric"><div class="label">Daily DD Remaining</div><div class="value ${dc}">${money(dr)}</div><div class="muted">${money(dailyUsed(x))} used today</div></div></div><div class="grid content-grid"><div class="panel"><div class="panel-head"><div><h3>Risk Monitor</h3><span class="muted">${esc(x.firm)} · ${esc(x.name)}</span></div>${statusBadge(x)}</div><div class="progress-row"><div class="progress-head"><span>Profit Target</span><b>${pct(fill(Math.max(0,b-x.startingBalance),x.target))} complete</b></div><div class="bar"><div class="fill" style="width:${fill(Math.max(0,b-x.startingBalance),x.target)}%"></div></div><div class="muted">${money(tr)} remaining</div></div><div class="progress-row"><div class="progress-head"><span>Maximum Drawdown</span><b>${pct(fill(mu,x.maxDd))} used</b></div><div class="bar"><div class="fill ${mc}" style="width:${fill(mu,x.maxDd)}%"></div></div><div class="muted">${money(mr)} remaining · ${x.ddMode} mode</div></div><div class="progress-row"><div class="progress-head"><span>Daily Drawdown</span><b>${pct(fill(dailyUsed(x),x.dailyDd))} used</b></div><div class="bar"><div class="fill ${dc}" style="width:${fill(dailyUsed(x),x.dailyDd)}%"></div></div><div class="muted">${money(dr)} remaining · resets ${esc(x.resetTime)}</div></div><div class="modal-actions" style="justify-content:flex-start"><button class="primary-btn" id="dashTrade">＋ Record Trade</button><button class="ghost-btn" id="dashAccounts">Manage Accounts</button></div></div><div class="panel"><div class="panel-head"><h3>Account Snapshot</h3>${statusBadge(x)}</div><div class="muted">Account size</div><div class="stat-big">${money(x.accountSize)}</div><div style="height:14px"></div><div class="muted">Starting balance</div><b>${money(x.startingBalance)}</b><div style="height:14px"></div><div class="muted">Maximum DD</div><b>${money(x.maxDd)}</b><div style="height:14px"></div><div class="muted">Daily DD</div><b>${money(x.dailyDd)}</b><div style="height:14px"></div><div class="muted">Total trades</div><b>${x.trades.length}</b></div></div><div class="panel" style="margin-top:14px"><div class="panel-head"><h3>Recent Trades</h3><button class="ghost-btn" id="dashHistory">View all</button></div>${table([...x.trades].sort((p,q)=>new Date(q.date)-new Date(p.date)).slice(0,7))}</div>`;$("#dashTrade").onclick=()=>view("trade");$("#dashAccounts").onclick=()=>view("accounts");$("#dashHistory").onclick=()=>view("history")}
function table(ts){if(!ts.length)return'<div class="empty"><strong>No trades yet</strong>Record your first trade.</div>';return`<div class="table-wrap"><table><thead><tr><th>Date</th><th>Pair</th><th>Side</th><th>P/L</th><th>Balance After</th><th>Note</th></tr></thead><tbody>${ts.map(t=>`<tr><td>${new Date(t.date).toLocaleString()}</td><td><b>${esc(t.pair)}</b></td><td>${t.side}</td><td class="${t.pnl>=0?"positive":"negative"}"><b>${t.pnl>=0?"+":""}${money(t.pnl)}</b></td><td>${money(t.balanceAfter)}</td><td>${esc(t.note||"—")}</td></tr>`).join("")}</tbody></table></div>`}
function renderAccounts(){let v=$("#accountsView");v.innerHTML=`<div class="panel"><div class="panel-head"><div><h3>Your Prop Accounts</h3><span class="muted">Each account has separate calculations.</span></div><button class="primary-btn" id="addAccount">＋ Add Account</button></div></div><div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(280px,1fr));margin-top:14px">${state.accounts.map(x=>`<div class="panel"><div class="panel-head"><div><h3>${esc(x.firm)}</h3><span class="muted">${esc(x.name)}</span></div>${statusBadge(x)}</div><div class="stat-big">${money(bal(x))}</div><div class="muted">Status: ${accountStatus(x)[2]}</div><div class="muted" style="margin-top:6px">Target ${money(x.target)} · Remaining ${money(targetRemaining(x))}</div><div class="muted" style="margin-top:6px">Max DD remaining ${money(maxRem(x))}</div><div class="muted" style="margin-top:6px">Daily DD remaining ${money(dailyRem(x))}</div><div class="modal-actions"><button class="primary-btn open-account" data-id="${x.id}">Open</button><button class="ghost-btn edit-account" data-id="${x.id}">Edit</button><button class="danger-btn del-account" data-id="${x.id}">Delete</button></div></div>`).join("")}</div>`;$("#addAccount").onclick=()=>openAccount();$$(".open-account").forEach(b=>b.onclick=()=>{state.selectedId=b.dataset.id;save();refreshAll();view("dashboard")});$$(".edit-account").forEach(b=>b.onclick=()=>openAccount(b.dataset.id));$$(".del-account").forEach(b=>b.onclick=()=>{pendingDelete=b.dataset.id;$("#deleteModal").classList.remove("hidden")})}
function renderTrade(){let v=$("#tradeView"),x=a();if(!x){v.innerHTML='<div class="empty">Add an account first.</div>';return}v.innerHTML=`<div class="grid trade-layout"><div class="panel"><div class="panel-head"><div><h3>Record Trade</h3><span class="muted">Nothing is saved until you click Save Trade.</span></div></div><form id="tradeForm"><div class="form-grid"><label>Date & Time<input id="tradeDate" type="datetime-local" value="${localDT()}" required></label><label>Pair / Symbol<input id="pair" placeholder="XAUUSD, BTCUSD, EURUSD" required></label><div><span class="muted">Direction</span><div class="direction" style="margin-top:7px"><input type="radio" name="side" id="buy" value="BUY" checked><label for="buy">BUY</label><input type="radio" name="side" id="sell" value="SELL"><label for="sell">SELL</label></div></div><label>P/L ($)<input id="pnl" type="number" step=".01" placeholder="-250 or 500" required></label><label>Entry Price<input id="entry" type="number" step="any"></label><label>Exit Price<input id="exit" type="number" step="any"></label><label>Lot / Size<input id="size" type="number" step="any"></label><label>Risk / Planned Loss ($)<input id="risk" type="number" step=".01"></label></div><label class="full">Trade Note<textarea id="tradeNote" rows="3" placeholder="Setup, reason, mistake, session..."></textarea></label><div class="modal-actions"><button type="button" class="ghost-btn" id="cancelTradeBtn">Cancel</button><button class="primary-btn" type="submit">Save Trade & Update Account</button></div></form></div><div class="panel"><div class="panel-head"><h3>Current Account</h3>${statusBadge(x)}</div><div class="muted">Balance</div><div class="stat-big">${money(bal(x))}</div><div style="height:18px"></div><div class="muted">Profit target remaining</div><div class="stat-big">${money(targetRemaining(x))}</div><div style="height:18px"></div><div class="muted">Max DD remaining</div><div class="stat-big">${money(maxRem(x))}</div><div style="height:18px"></div><div class="muted">Daily DD remaining</div><div class="stat-big">${money(dailyRem(x))}</div></div></div>`;$("#cancelTradeBtn").onclick=()=>view("dashboard");$("#tradeForm").onsubmit=saveTrade}
function saveTrade(e){e.preventDefault();let x=a(),p=Number($("#pnl").value);let t={id:uid(),date:new Date($("#tradeDate").value).toISOString(),pair:$("#pair").value.trim().toUpperCase(),side:document.querySelector('input[name="side"]:checked').value,pnl:p,entry:Number($("#entry").value)||null,exit:Number($("#exit").value)||null,size:Number($("#size").value)||null,risk:Number($("#risk").value)||null,note:$("#tradeNote").value.trim()};t.balanceAfter=bal(x)+p;x.trades.push(t);save();refreshAll();toast("Trade recorded — account updated.");view("dashboard")}
function renderHistory(){let x=a();$("#historyView").innerHTML=`<div class="panel"><div class="panel-head"><h3>Trade History</h3><button class="primary-btn" id="historyTrade">＋ New Trade</button></div>${table(x?[...x.trades].sort((p,q)=>new Date(q.date)-new Date(p.date)):[])}</div>`;$("#historyTrade").onclick=()=>view("trade")}
function renderStats(){let x=a(),ts=x?.trades||[],w=ts.filter(t=>t.pnl>0),l=ts.filter(t=>t.pnl<0),net=ts.reduce((s,t)=>s+t.pnl,0),gw=w.reduce((s,t)=>s+t.pnl,0),gl=Math.abs(l.reduce((s,t)=>s+t.pnl,0)),pf=gl?gw/gl:(gw?Infinity:0);$("#statsView").innerHTML=`<div class="grid stats-grid"><div class="panel"><div class="muted">Net P/L</div><div class="stat-big ${net>=0?"positive":"negative"}">${net>=0?"+":""}${money(net)}</div></div><div class="panel"><div class="muted">Win Rate</div><div class="stat-big">${ts.length?pct(w.length/ts.length*100):"0.00%"}</div></div><div class="panel"><div class="muted">Profit Factor</div><div class="stat-big">${pf===Infinity?"∞":pf.toFixed(2)}</div></div><div class="panel"><div class="muted">Best Trade</div><div class="stat-big positive">${ts.length?money(Math.max(...ts.map(t=>t.pnl))):money(0)}</div></div><div class="panel"><div class="muted">Worst Trade</div><div class="stat-big negative">${ts.length?money(Math.min(...ts.map(t=>t.pnl))):money(0)}</div></div><div class="panel"><div class="muted">Trades</div><div class="stat-big">${ts.length}</div></div></div>`}
function renderSettings(){$("#settingsView").innerHTML=`<div class="panel"><h3>Data & Storage</h3><p class="muted">Your data is saved locally for instant loading and synced to Supabase through a Netlify server function. Cloud status: <b>${cloudReady?"Connected":"Offline / local only"}</b></p><div class="modal-actions" style="justify-content:flex-start"><button class="primary-btn" id="export">Export JSON</button><button class="ghost-btn" id="imp">Import JSON</button><button class="danger-btn" id="reset">Reset Local Data</button></div><input id="file" type="file" accept="application/json" hidden></div>`;$("#export").onclick=()=>{let b=new Blob([JSON.stringify(state,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download="proptrack-backup.json";a.click()};$("#imp").onclick=()=>$("#file").click();$("#file").onchange=async e=>{try{state=JSON.parse(await e.target.files[0].text());save();refreshAll();toast("Backup imported.")}catch{toast("Invalid backup.")}};$("#reset").onclick=()=>{if(confirm("Delete local data and start fresh? Cloud data is not deleted.")){localStorage.removeItem(KEY);state=defaultState();saveLocal();refreshAll();toast("Local data reset.")}}}
function pctOrUsd(value,type){return type==="percent"?value/100: value}
function openAccount(id){let x=id?state.accounts.find(a=>a.id===id):null;$("#accountModalTitle").textContent=x?"Edit Prop Account":"Add Prop Account";$("#editAccountId").value=x?.id||"";$("#firm").value=x?.firm||"";$("#accountName").value=x?.name||"";$("#accountSize").value=x?.accountSize??"";$("#startingBalance").value=x?.startingBalance??"";$("#targetType").value=x?.targetType||"percent";$("#targetInput").value=x?.targetInput??8;$("#maxDdType").value=x?.maxDdType||"percent";$("#maxDdInput").value=x?.maxDdInput??10;$("#dailyDdType").value=x?.dailyDdType||"percent";$("#dailyDdInput").value=x?.dailyDdInput??5;$("#resetTime").value=x?.resetTime||"00:00";$("#ddMode").value=x?.ddMode||"static";$("#accountNotes").value=x?.notes||"";$("#accountModal").classList.remove("hidden")}
$("#accountForm").onsubmit=e=>{e.preventDefault();let id=$("#editAccountId").value,size=+$("#accountSize").value,start=+$("#startingBalance").value,targetType=$("#targetType").value,targetInput=+$("#targetInput").value,maxDdType=$("#maxDdType").value,maxDdInput=+$("#maxDdInput").value,dailyDdType=$("#dailyDdType").value,dailyDdInput=+$("#dailyDdInput").value;let d={firm:$("#firm").value.trim(),name:$("#accountName").value.trim(),accountSize:size,startingBalance:start,targetType,targetInput,target:targetType==="percent"?start*targetInput/100:targetInput,maxDdType,maxDdInput,maxDd:maxDdType==="percent"?size*maxDdInput/100:maxDdInput,dailyDdType,dailyDdInput,dailyDd:dailyDdType==="percent"?size*dailyDdInput/100:dailyDdInput,resetTime:$("#resetTime").value,ddMode:$("#ddMode").value,notes:$("#accountNotes").value};if(id)Object.assign(state.accounts.find(x=>x.id===id),d);else{let x={id:uid(),...d,trades:[]};state.accounts.push(x);state.selectedId=x.id}save();close("accountModal");refreshAll();toast("Account saved to cloud.")}
$("#confirmDelete").onclick=()=>{state.accounts=state.accounts.filter(x=>x.id!==pendingDelete);state.selectedId=state.accounts[0]?.id||null;pendingDelete=null;save();close("deleteModal");refreshAll();toast("Account deleted.")}
$$("[data-close-modal]").forEach(b=>b.onclick=()=>close(b.dataset.closeModal));function close(id){$("#"+id).classList.add("hidden")}
function view(n){$$(".view").forEach(x=>x.classList.remove("active-view"));$("#"+n+"View").classList.add("active-view");$$(".nav-item").forEach(x=>x.classList.toggle("active",x.dataset.view===n));$("#pageTitle").textContent={dashboard:"Dashboard",accounts:"Accounts",trade:"New Trade",history:"Trade History",stats:"Statistics",settings:"Settings"}[n];render(n)}
function render(n){if(n==="dashboard")renderDashboard();if(n==="accounts")renderAccounts();if(n==="trade")renderTrade();if(n==="history")renderHistory();if(n==="stats")renderStats();if(n==="settings")renderSettings()}
function refreshAll(){refreshSwitch();let n=document.querySelector(".active-view")?.id.replace("View","")||"dashboard";render(n)}
function toast(s){let t=$("#toast");t.textContent=s;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),2200)}
$$(".nav-item").forEach(b=>b.onclick=()=>view(b.dataset.view));$("#accountSwitcher").onchange=e=>{state.selectedId=e.target.value;save();refreshAll()};$("#quickTradeBtn").onclick=()=>view("trade");refreshAll();initCloud();
